/// @description Room Start Layer Initialization
// Create Default Layers for Lighting Engine

// Create Basic Layers
if (lighting_engine_layer_main_index == -1)
{
    lighting_engine_add_layer(LightingEngineUnitLayer, 0);
}